<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itemDao">
    <select id="getById" resultType="com.xie.bean.Item">
        SELECT * FROM goods
        where id=#{id}
    </select>
</mapper>